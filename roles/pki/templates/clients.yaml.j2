---
{% for client, certs in client_certs.items() | sort %}
{{ client }}:
{% for cert, content in certs.items() %}
{% set tag = '!vault |' if (content | is_vault_string) else '|' %}
  {{ cert }}: {{ tag }}
    {{ content | trim | indent(width=4) }}
{% endfor %}
{% endfor %}